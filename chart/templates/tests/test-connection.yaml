apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "goldpinger.fullname" . }}-test-connection"
  labels:
    {{- include "goldpinger.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "goldpinger.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
